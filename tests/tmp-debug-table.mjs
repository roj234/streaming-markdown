import * as md from './streaming-markdown-parser.js';

const names=Object.fromEntries(Object.entries(md).filter(([k,v])=>typeof v==='number').map(([k,v])=>[v,k]));let step=0;const r={add_text(t,p){console.log(step,'text',JSON.stringify(t),'tok',names[p.token],'stack',[...p.tokens].map(x=>names[x]).join('>'),'pending',JSON.stringify(p.pending),'bq',p.blockquote_idx,'table',p.table_state)},add_token(tok,p){console.log(step,'start',names[tok],'stack',[...p.tokens].map(x=>names[x]).join('>'),'pending',JSON.stringify(p.pending),'bq',p.blockquote_idx,'table',p.table_state)},end_token(tok,p){console.log(step,'end',names[tok],'stack',[...p.tokens].map(x=>names[x]).join('>'),'pending',JSON.stringify(p.pending),'bq',p.blockquote_idx,'table',p.table_state);return ''},set_attr(a,v){console.log(step,'attr',a,v)}};const p=md.createMarkdownParser(r,{});const s='> | a | b |\n> |---|---|\n';for(const ch of s){step++; console.log('\nCHAR',step,JSON.stringify(ch)); p.write(ch)} p.end();